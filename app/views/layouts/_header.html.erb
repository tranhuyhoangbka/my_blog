<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag("logo.jpg", class: "img-responsive logo"), root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#">Trang chủ</a></li>
        <li><a href="#">Giới thiệu</a></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Home Admin", rails_admin.dashboard_path %></li>
              <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% end %>
        <li><a href="#"><%= image_tag "anhdong.gif", size: "48", class: "img-responsive funy" %></a></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="slider-wrap">
  <div class="container">
    <div class="row">
    <div class="hidden-xs hidden-sm col-md-12">
    <div id="myCarousel" class="carousel slide" data-ride="carousel">
        <!-- Wrapper for slides -->
      <div class="carousel-inner" role="listbox">
        <div class="item active">
          <a href="#"><%= image_tag "slider1.jpg", alt: "Chania", class: "img-responsive img-rounded" %></a>
          <div class="carousel-caption">
            <h3>Chania</h3>
            <p>The atmosphere in Chania has a touch of Florence and Venice.</p>
          </div>
        </div>

        <div class="item">
          <a href="#"><%= image_tag "slider2.jpg", alt: "Chania", class: "img-responsive img-rounded" %></a>
          <div class="carousel-caption">
            <h3>Chania</h3>
            <p>The atmosphere in Chania has a touch of Florence and Venice.</p>
          </div>
        </div>

        <div class="item">
          <a href="#"><%= image_tag "slider3.jpg", alt: "Flower", class: "img-responsive img-rounded" %></a>
          <div class="carousel-caption">
            <h3>Flowers</h3>
            <p>Beatiful flowers in Kolymbari, Crete.</p>
          </div>
        </div>

        <div class="item">
          <a href="#"><%= image_tag "slider4.jpg", alt: "Flower", class: "img-responsive img-rounded" %></a>
          <div class="carousel-caption">
            <h3>Flowers</h3>
            <p>Beatiful flowers in Kolymbari, Crete.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
</div>

<div class="top-menu" style="margin-top: 0px;">
  <div class="container">
    <ul class="nav nav-tabs">
      <li><a href="#"><i class="glyphicon glyphicon-home text-primary"></i></a></li>
      <% all_categories.each do |category| %>
        <%= content_tag :li, link_to(category.name, category_path(category)) %>
      <% end %>
    </ul>
  </div>
</div>
